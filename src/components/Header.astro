---
// Header.astro - Sticky header with language toggle and Global 100 awards
---

<header class="site-header" id="site-header">
  <div class="header-inner">

    <!-- Logo -->
    <a href="/" class="logo-link" aria-label="Sigvardsson Consulting Group">
      <img src="/images/logos/S_blue.png" alt="SCG" class="header-logo" width="44" height="44" />
      <span class="logo-text">
        <span class="logo-sigvardsson">Sigvardsson</span>
        <span class="logo-consulting sv-text">Consulting Group</span>
        <span class="logo-consulting en-text">Consulting Group</span>
      </span>
    </a>

    <!-- Desktop Navigation -->
    <nav class="desktop-nav" aria-label="Main navigation">
      <a href="/" class="nav-link">
        <span class="sv-text">Hem</span>
        <span class="en-text">Home</span>
      </a>
      <a href="/services/" class="nav-link">
        <span class="sv-text">Våra tjänster</span>
        <span class="en-text">Our Services</span>
      </a>
      <a href="/our-process/" class="nav-link">
        <span class="sv-text">Vår approach</span>
        <span class="en-text">Our Approach</span>
      </a>
      <a href="/our-people/" class="nav-link">
        <span class="sv-text">Vårt team</span>
        <span class="en-text">Our Team</span>
      </a>
      <a href="/Contact/" class="nav-link nav-cta">
        <span class="sv-text">Kontakt</span>
        <span class="en-text">Contact</span>
      </a>
    </nav>

    <!-- Right side: Awards + Lang + Hamburger -->
    <div class="header-right">

      <!-- Global 100 Awards (desktop) -->
      <div class="awards-strip desktop-only" aria-label="Global 100 Awards">
        <div class="award-wrap"><img src="/images/awards/global_100_2024.png" alt="Global 100 Winner 2024" class="award-badge" /></div>
        <div class="award-wrap"><img src="/images/awards/global_100_2025.png" alt="Global 100 Winner 2025" class="award-badge" /></div>
        <div class="award-wrap"><img src="/images/awards/global_100_2026.png" alt="Global 100 Winner 2026" class="award-badge" /></div>
      </div>

      <!-- Language Toggle -->
      <button
        class="lang-toggle"
        id="lang-toggle"
        aria-label="Switch language"
        title="Switch language / Byt språk"
      >
        <span class="lang-sv sv-text">EN</span>
        <span class="lang-en en-text">SV</span>
      </button>

      <!-- Hamburger (mobile) -->
      <button class="hamburger" id="hamburger" aria-label="Open menu" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div class="mobile-menu" id="mobile-menu" aria-hidden="true">
    <nav class="mobile-nav">
      <a href="/" class="mobile-nav-link">
        <span class="sv-text">Hem</span><span class="en-text">Home</span>
      </a>
      <a href="/services/" class="mobile-nav-link">
        <span class="sv-text">Våra tjänster</span><span class="en-text">Our Services</span>
      </a>
      <a href="/our-process/" class="mobile-nav-link">
        <span class="sv-text">Vår approach</span><span class="en-text">Our Approach</span>
      </a>
      <a href="/our-people/" class="mobile-nav-link">
        <span class="sv-text">Vårt team</span><span class="en-text">Our Team</span>
      </a>
      <a href="/Contact/" class="mobile-nav-link mobile-nav-cta">
        <span class="sv-text">Kontakt</span><span class="en-text">Contact</span>
      </a>

      <!-- Awards in mobile menu -->
      <div class="mobile-awards">
        <div class="award-wrap award-wrap-mobile"><img src="/images/awards/global_100_2024.png" alt="Global 100 Winner 2024" class="mobile-award-badge" /></div>
        <div class="award-wrap award-wrap-mobile"><img src="/images/awards/global_100_2025.png" alt="Global 100 Winner 2025" class="mobile-award-badge" /></div>
        <div class="award-wrap award-wrap-mobile"><img src="/images/awards/global_100_2026.png" alt="Global 100 Winner 2026" class="mobile-award-badge" /></div>
      </div>
    </nav>
  </div>
</header>

<style>
  /* ===== HEADER ===== */
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(255,255,255,0.97);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(1,13,96,0.08);
    transition: box-shadow 0.3s ease;
  }

  .site-header.scrolled {
    box-shadow: 0 4px 20px rgba(1,13,96,0.12);
  }

  .header-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    height: 70px;
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  /* ===== LOGO ===== */
  .logo-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-shrink: 0;
    text-decoration: none;
  }

  .logo-img {
    width: 44px;
    height: 44px;
    object-fit: contain;
    transition: opacity 0.2s;
  }

  /* Header logo — S_blue.png has white background, navy logo */
  .header-logo {
    width: 44px;
    height: 44px;
    object-fit: contain;
    flex-shrink: 0;
  }

  .logo-text {
    display: flex;
    flex-direction: column;
    line-height: 1.15;
  }

  .logo-sigvardsson {
    font-family: 'Chakra Petch', sans-serif;
    font-weight: 600;
    font-size: 1.05rem;
    color: var(--color-navy);
    letter-spacing: 0.02em;
  }

  .logo-consulting {
    font-family: 'Nunito', sans-serif;
    font-weight: 200;
    font-size: 0.72rem;
    color: var(--color-blue-mid);
    letter-spacing: 0.06em;
  }

  /* ===== DESKTOP NAV ===== */
  .desktop-nav {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    flex: 1;
    justify-content: center;
  }

  .nav-link {
    font-family: 'Chakra Petch', sans-serif;
    font-weight: 500;
    font-size: 0.82rem;
    letter-spacing: 0.04em;
    color: var(--color-navy-mid);
    padding: 0.45rem 0.75rem;
    border-radius: 6px;
    transition: background 0.2s, color 0.2s;
    white-space: nowrap;
  }

  .nav-link:hover {
    background: var(--color-off-white);
    color: var(--color-navy);
  }

  .nav-cta {
    background: var(--color-navy);
    color: var(--color-white) !important;
    padding: 0.45rem 1.1rem;
  }

  .nav-cta:hover {
    background: var(--color-blue) !important;
    color: var(--color-white) !important;
  }

  /* ===== RIGHT SIDE ===== */
  .header-right {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-shrink: 0;
  }

  /* ===== AWARDS ===== */
  .awards-strip {
    display: flex;
    align-items: center;
    gap: 0.35rem;
  }

  /* Award wrap normalizes different badge sizes */
  .award-wrap {
    width: 38px;
    height: 38px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .award-wrap-mobile {
    width: 56px;
    height: 56px;
  }

  .award-badge,
  .mobile-award-badge {
    width: 100%;
    height: 100%;
    object-fit: contain;
    opacity: 0.88;
    transition: opacity 0.2s, transform 0.2s;
    cursor: default;
    display: block;
  }

  .award-wrap:hover .award-badge,
  .award-wrap:hover .mobile-award-badge {
    opacity: 1;
    transform: scale(1.08);
  }

  /* ===== LANGUAGE TOGGLE ===== */
  .lang-toggle {
    font-family: 'Chakra Petch', sans-serif;
    font-weight: 500;
    font-size: 0.78rem;
    letter-spacing: 0.08em;
    color: var(--color-navy);
    background: var(--color-off-white);
    border: 1px solid rgba(1,13,96,0.15);
    border-radius: 6px;
    padding: 0.3rem 0.7rem;
    cursor: pointer;
    transition: all 0.2s;
    flex-shrink: 0;
  }

  .lang-toggle:hover {
    background: var(--color-navy);
    color: var(--color-white);
    border-color: var(--color-navy);
  }

  /* ===== HAMBURGER ===== */
  .hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
  }

  .hamburger span {
    display: block;
    width: 24px;
    height: 2px;
    background: var(--color-navy);
    transition: all 0.3s ease;
    transform-origin: center;
  }

  .hamburger.open span:nth-child(1) { transform: rotate(45deg) translate(5px, 5px); }
  .hamburger.open span:nth-child(2) { opacity: 0; transform: scaleX(0); }
  .hamburger.open span:nth-child(3) { transform: rotate(-45deg) translate(5px, -5px); }

  /* ===== MOBILE MENU ===== */
  .mobile-menu {
    display: none;
    background: var(--color-white);
    border-top: 1px solid rgba(1,13,96,0.08);
    overflow: hidden;
    max-height: 0;
    transition: max-height 0.4s ease;
  }

  .mobile-menu.open {
    max-height: 600px;
  }

  .mobile-nav {
    padding: 1rem 2rem 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .mobile-nav-link {
    font-family: 'Chakra Petch', sans-serif;
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--color-navy-mid);
    padding: 0.75rem 0;
    border-bottom: 1px solid rgba(1,13,96,0.06);
    transition: color 0.2s;
  }

  .mobile-nav-link:hover { color: var(--color-navy); }

  .mobile-nav-cta {
    margin-top: 0.75rem;
    background: var(--color-navy);
    color: var(--color-white) !important;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    text-align: center;
    border-bottom: none !important;
  }

  .mobile-awards {
    display: flex;
    gap: 0.75rem;
    padding-top: 1.25rem;
    justify-content: center;
  }

  .mobile-award-badge {
    width: 52px;
    height: 52px;
    object-fit: contain;
  }

  /* ===== DESKTOP-ONLY ===== */
  .desktop-only { display: flex; }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 1024px) {
    .awards-strip { display: none !important; }
  }

  @media (max-width: 768px) {
    .desktop-nav { display: none; }
    .hamburger { display: flex; }
    .mobile-menu { display: block; }
    .header-inner { padding: 0 1.25rem; }

    .logo-consulting { display: none; }
  }
</style>

<script>
  // ===== SCROLL SHADOW =====
  const header = document.getElementById('site-header');
  window.addEventListener('scroll', () => {
    header?.classList.toggle('scrolled', window.scrollY > 20);
  });

  // ===== HAMBURGER MENU =====
  const hamburger = document.getElementById('hamburger');
  const mobileMenu = document.getElementById('mobile-menu');

  hamburger?.addEventListener('click', () => {
    const isOpen = hamburger.classList.toggle('open');
    mobileMenu?.classList.toggle('open', isOpen);
    hamburger.setAttribute('aria-expanded', String(isOpen));
    mobileMenu?.setAttribute('aria-hidden', String(!isOpen));
  });

  // ===== LANGUAGE TOGGLE =====
  const langToggle = document.getElementById('lang-toggle');

  langToggle?.addEventListener('click', () => {
    const html = document.documentElement;
    const current = html.getAttribute('data-lang') || 'sv';
    const next = current === 'sv' ? 'en' : 'sv';
    html.setAttribute('data-lang', next);
    localStorage.setItem('scg-lang', next);

    // Update meta for SEO (optional, for single-page reference)
    document.documentElement.setAttribute('lang', next);
  });
</script>
